version: "3.9"

services:
  pipeline:
    build: .
    image: visdrone-pipeline:latest
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./logs:/app/logs
      - ./src:/app/src
      - ./configs:/app/configs
    environment:
      - PYTHONUNBUFFERED=1